<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="service.css" rel='stylesheet' type='text/css' />
    <title>Cuts By De'Meco</title>
</head>

<body>

    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">
                <center>Cuts By De'Meco</center>
            </h1>
            <p class="lead">
                <center>Gentlemen's Cuts For Affordable Prices. </center>
            </p>

        </div>
    </div>
    <form id="barberForm" action="" method="POST">
        <div id="name" class="container">
            <h1>
                <center>Select a Service</center>
            </h1>
            <% for(var i = 0; i < response.length; i++) { %>
            <input id="service" type="radio" name="service" value="<%= response[i]._id %> ">
            <span class="checkmark"></span>
            <span id="serviceName" class="item-price"> <%= response[i].name %> </span>
            <span id="price" class="item-price"> <%= response[i].price %> </span>
            <span id="time" class="item-price"> <%= response[i].time %> </span>
            <% } %>

        </div>

        <div class="row justify-content-center">
            <button id="serviceBtn" type="submit" class="btn-primary.custom-btn">Continue</button>
        </div>

        <div id="calender">
            <input id="date" onchange="calender()" type="date" name="appt" min="2019-04-09">

        </div>
        <div id="container-contact100">
            <div class="wrap-contact100">
                <span class="contact100-form-title">
                    Enter Customer Information
                </span>

                <div class="wrap-input100  bg1">
                    <span class="label-input100">FULL NAME *</span>
                    <input class="input100" id="name" type="text" name="name" placeholder="Enter Your Name">
                </div>

                <div class="wrap-input100  bg1 rs1-wrap-input100">
                    <span class="label-input100">Email *</span>
                    <input class="input100" type="text" name="email" id="email" placeholder="Enter Your Email ">
                </div>

                <div class="wrap-input100 bg1 rs1-wrap-input100">
                    <span class="label-input100">Phone *</span>
                    <input class="input100" id="phone" type="text" name="phone" placeholder="Enter Number Phone">
                </div>

            </div>
            <input type="button" value="Book" type="submit">
    </form>
</body>
<script src="service.js"></script>

<script>
    var result;
    fetch('http://localhost:3000/times/')
        .then(res => res.json()) //filter blah // build the radio options
        .then(response => {
            result = response
            console.log(result)
        })
        .catch(error => console.error('Error:', error));

    const calender = () => {
        const calenderData = document.getElementById("date").value;
        //had to hard code, moment is causing error

        fetch('http://localhost:3000/appointments/' + 1559188800, {
            method: "GET",
            headers: {
                'Content-Type': 'application/json',
            },
        })
            .then(res => res.json()) //filter blah // build the radio options
            .then(response => {
                for (var i = 0; i < response.length; i++) {
                    console.log(response[i])

                }
            })
            .catch(error => console.error('Error:', error));
    }

</script>



</html>